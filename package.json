{
  "name": "javaneh-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build && rm -rf dist && cp -r web/dist dist"
  }
}
